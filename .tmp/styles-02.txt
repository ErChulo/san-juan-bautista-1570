crumbs { display: flex; flex-wrap: wrap; gap: 8px; align-items: center; color: rgba(255,255,255,.6); font-size: .86rem; }
.breadcrumbs a { color: rgba(255,255,255,.8); text-decoration: none; }
.page-hero h1 { font-size: clamp(3.2rem, 8vw, 7rem); max-width: 1000px; }
.page-hero .subtitle { max-width: 800px; color: rgba(255,255,255,.72); font-size: 1.12rem; }
.tag-row { margin-top: 24px; display: flex; flex-wrap: wrap; gap: 9px; }
.tag { padding: 7px 10px; border-radius: 999px; background: rgba(255,255,255,.09); border: 1px solid rgba(255,255,255,.14); font-size: .78rem; font-weight: 700; }

.fact-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; margin-top: -34px; position: relative; z-index: 2; }
.fact { background: #fffaf2; border: 1px solid var(--line); border-radius: 16px; padding: 20px; box-shadow: 0 14px 34px rgba(20,38,43,.08); }
.fact .label { display: block; color: var(--ink-soft); font-size: .76rem; text-transform: uppercase; letter-spacing: .09em; font-weight: 800; }
.fact strong { display: block; margin-top: 8px; font-family: Georgia, serif; font-size: 1.55rem; font-weight: 600; }

.island-map-panel { min-height: 480px; border-radius: var(--radius-lg); overflow: hidden; background: var(--paper-2); border: 1px solid var(--line); }
.island-map-panel svg { width: 100%; min-height: 480px; display: block; }

.status-card { border-left: 4px solid var(--gold); background: rgba(255,255,255,.55); padding: 24px 26px; border-radius: 0 16px 16px 0; }
.status-card[data-level="alto"] { border-left-color: var(--sage); }
.status-card[data-level="medio"] { border-left-color: var(--gold); }
.status-card[data-level="condicional"] { border-left-color: var(--terracotta); }
.status-card h3 { margin: 0 0 8px; font-family: Georgia, serif; font-size: 1.45rem; }
.status-card p { margin: 0; color: var(--ink-soft); }

.prev-next { display: grid; grid-template-columns: 1fr 1fr; gap: 12px; margin-top: 42px; }
.prev-next a { text-decoration: none; border: 1px solid var(--line); border-radius: 16px; padding: 18px; background: rgba(255,255,255,.5); }
.prev-next .next { text-align: right; }
.prev-next small { display: block; color: var(--ink-soft); margin-bottom: 5px; }
.prev-next strong { font-family: Georgia, serif; font-size: 1.2rem; }

.method-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-top: 30px; }
.method-step { padding: 24px; border: 1px solid var(--line); border-radius: 18px; background: rgba(255,255,255,.52); }
.method-step .num { font: 800 .78rem/1 ui-monospace, monospace; color: var(--terracotta); }
.method-step h3 { font-family: Georgia, serif; font-size: 1.35rem; margin: 26px 0 8px; }
.method-step p { color: var(--ink-soft); margin: 0; font-size: .94rem; }

.source-list { display: grid; gap: 12px; margin-top: 28px; }
.source-item { display: grid; grid-template-columns: 140px 1fr; gap: 22px; padding: 20px 0; border-top: 1px solid var(--line); }
.source-item:last-child { border-bottom: 1px solid var(--line); }
.source-type { font-size: .75rem; text-transform: uppercase; letter-spacing: .1em; font-weight: 850; color: var(--terracotta); }
.source-item h3 { margin: 0 0 5px; font-family: Georgia, serif; font-size: 1.28rem; }
.source-item p { margin: 0; color: var(--ink-soft); }
.source-item a { text-underline-offset: 3px; }

.footer { margin-top: 80px; background: #102c35; color: rgba(255,255,255,.76); }
.footer-inner { max-width: var(--content); margin: 0 auto; padding: 42px 24px; display: flex; gap: 24px; justify-content: space-between; align-items: flex-end; }
.footer strong { display: block; color: #fff; font-family: Georgia, serif; font-size: 1.15rem; margin-bottom: 6px; }
.footer p { margin: 0; max-width: 690px; font-size: .87rem; }
.footer a { color: #fff; text-underline-offset: 3px; }

.not-found { min-height: 65vh; display: grid; place-items: center; padding: 40px 24px; text-align: center; }
.not-found h1 { font-family: Georgia, serif; font-size: 4rem; margin: 0 0 12px; }

@media (max-width: 980px) {
  .hero-inner, .two-col { grid-template-columns: 1fr; }
  .hero-inner { padding-top: 46px; }
  .metric-grid, .fact-grid { grid-template-columns: repeat(2, 1fr); }
  .island-grid, .method-grid { grid-template-columns: repeat(2, 1fr); }
  .map-card { min-height: 430px; }
  .map-svg { min-height: 430px; }
}
@media (max-width: 720px) {
  .nav { padding: 0 16px; gap: 10px; }
  .brand span:last-child { display: none; }
  .nav-link { padding: 8px 10px; font-size: .84rem; }
  .hero { border-radius: 0 0 28px 28px; }
  .hero-inner { padding: 42px 18px; min-height: auto; gap: 32px; }
  .hero h1 { font-size: clamp(3.1rem, 17vw, 5.3rem); }
  .section { padding: 62px 18px; }
  .metric-grid, .fact-grid, .island-grid, .method-grid, .prev-next { grid-template-columns: 1fr; }
  .fact-grid { margin-top: 0; padding-top: 18px; }
  .page-hero-inner { padding: 46px 18px 38px; }
  .page-hero h1 { font-size: clamp(3rem, 16vw, 5.5rem); }
  .source-item { grid-template-columns: 1fr; gap: 